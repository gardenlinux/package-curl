pkg=curl
version_orig=8.18.0
version="$version_orig-2"
version_suffix=gl1+bp1592

git_src -b "debian/$version" "https://salsa.debian.org/debian/curl.git"

# We are changing control and rules files invalidating available symbols
apply_patches debian_fixes
rm ${dir}/src/debian/libcurl*.symbols
