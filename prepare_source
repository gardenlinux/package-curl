pkg=curl
version_orig=8.20.0
version="$version_orig-5"
git_src -b "debian/$version_orig-5" "https://salsa.debian.org/debian/curl.git"

#removing rtmp
sed -i '/librtmp-dev/d;s/RTMP, //' "$dir/src/debian/control"

apply_patches
